// Staging route for the onboarding quiz funnel. Renders the real /onboarding
// surface so the full funnel can be exercised in production under /preview/onboarding.
export { default } from "../onboarding";
